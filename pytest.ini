[pytest]
testpaths = tests
pythonpath =
    .
    src
markers =
    windows_smoke: native-Windows UniProp integration smoke profile
    real_uniprop: real Uni-Core/Uni-Mol+/checkpoint profile checks
    cuda: checks that require or model CUDA readiness
